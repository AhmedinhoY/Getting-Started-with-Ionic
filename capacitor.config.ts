import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ahmedinho.ionicapp',
  appName: 'Ionic App',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
